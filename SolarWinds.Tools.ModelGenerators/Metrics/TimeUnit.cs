namespace SolarWinds.Tools.ModelGenerators.Metrics
{
    public enum TimeUnit
    {
        Milliseconds = 1,
        Seconds = 1000,
        Minutes = 1000*60,
        Hours = 1000*3600,
        Days = 1000*60*24,
    }
}
